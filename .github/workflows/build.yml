name: Build ZMK firmware
on:
  workflow_dispatch:
  push:
    paths-ignore:
      - "keymap-drawer/**"

jobs:
  build:
    # Keep the build workflow aligned with the ZMK version in config/west.yml.
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3.0

