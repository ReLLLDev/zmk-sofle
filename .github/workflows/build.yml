name: Build ZMK firmware
on:
  workflow_dispatch:
  push:
    paths-ignore:
      - "keymap-drawer/**"

jobs:
  build:
    # Local copy of the v0.3.0 build flow with Node 24 actions.
    uses: ./.github/workflows/build-user-config.yml

